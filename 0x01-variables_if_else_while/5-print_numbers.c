#include <stdio.h>

/**
 * main - Prints single digits of base 10 starting from 0
 * Return: 0 for success
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
