#include "main.h"

/**
 * _isdigit - Checks if a char is a digit (0-9)
 * @c: Takes in a char
 * Return: 1 if a char is a digit. Otherwise 0.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
